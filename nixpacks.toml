[phases.setup]
nixPkgs = ['python311', 'gcc', 'gdb', 'gmp', 'libffi', 'zlib', 'curl']

[phases.install]
cmds = [
  'python -m pip install --upgrade pip',
  'pip install -r requirements.txt'
]

[phases.build]
cmds = [
  'python -c "from app import init_database; init_database()"'
]

[start]
cmd = 'bash start.sh'
